{
  "name": "markdownToDoc",
  "version": "1.0.0",
  "description": "- get the list of .md files in the posts folder - I format each file into a .html version and save it to disk - I generate the index by rendering an index template - I copy over all static assets to the output directory",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "markdown-it": "^11.0.1"
  }
}
